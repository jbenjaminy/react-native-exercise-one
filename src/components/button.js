import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Button = () => (
        <TouchableOpacity>
            <Text>Purchase</Text>
        </TouchableOpacity>
);

export default Button;
